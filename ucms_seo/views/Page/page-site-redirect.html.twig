{% extends 'module:udashboard:views/Page/page.html.twig' %}

{% block item %}
  <tr>
    <td>{{ item.path }}</td>
    <td></td>
    <td>
      {{ udashboard_actions(item) }}
    </td>
  </tr>
{% endblock %}

{% block item_list %}
  <table class="table table-striped table-condensed">
    <thead>
      <tr>
        <th>{{ "Old path"|trans }}</th>
        <th>{{ "Content"|trans }}</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {% for item in items %}
        {{ block('item') }}
      {% else %}
        <tr>
          <td colspan="3">
            {{ "This site contains not SEO redirect."|trans }}
          </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% endblock %}

{#
            if (isset($nodes[$item->nid])) {
                $nodeLabel = $nodes[$item->nid]->getTitle();
            }
            else {
                $nodeLabel = $this->t("None");
            }

            $rows[] = [
                check_plain($item->path),
                isset($nodes[$item->nid]) ? l($nodeLabel, 'node/'.$item->nid) : $nodeLabel,
                theme('udashboard_actions', ['actions' => $this->getActions($item), 'mode' => 'icon']),
            ];
 #}
