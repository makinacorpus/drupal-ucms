{% extends 'module:udashboard:views/Page/page.html.twig' %}

{% block item %}
  <tr>
    <td>{{ item.mail|escape }}</td>
    <td>{{ item.displayName|escape }}</td>
    <td>{% if item.status %}{{ "Yes"|trans }}{% else %}{{ "No"|trans }}{% endif %}</td>
    <td>@todo</td>
    <td>{{ item.created|time_diff }}</td>
    <td>{% if item.login %}{{ item.login|time_diff }}{% else %}{{ "Never"|trans }}{% endif %}</td>
    <td>
      {{ udashboard_actions(item) }}
    </td>
  </tr>
{% endblock %}

{% block item_list %}
  <table class="table table-striped table-condensed">
    <thead>
      <tr>
        <th>{{ "Email"|trans }}</th>
        <th>{{ "Name"|trans }}</th>
        <th>{{ "Status"|trans }}</th>
        <th>{{ "Is webmaster?"|trans }}</th>
        <th>{{ "Created"|trans }}</th>
        <th>{{ "Last connection"|trans }}</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {% for item in items %}
        {{ block('item') }}
      {% endfor %}
    </tbody>
  </table>
{% endblock %}
