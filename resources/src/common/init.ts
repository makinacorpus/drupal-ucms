
// @todo not sure it meant to be used that way
declare const Drupal: any;

// Drupal attach point
Drupal.behaviors.UcmsInit = {
    // Theme select2 library
//    jQuery(function () {
//        jQuery('select').select2({theme: 'bootstrap'});
//    });
};
